# WitnessOS - Environment Configuration
# Copy to .env and fill in your values
#   cp .env.example .env

# Credential encryption key (generate with: openssl rand -hex 32)
WITNESSOS_CREDENTIAL_KEY=

# HMAC shared secret for agent→gateway authentication
WITNESSOS_CALLER_SECRET=
